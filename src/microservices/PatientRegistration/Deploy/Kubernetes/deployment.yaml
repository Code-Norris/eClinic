apiVersion: apps/v1
kind: Deployment
metadata:
  name: deploy-api-pr
  namespace: eclinic
  labels:
    app: api-pr
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-pr
  template:
    metadata:
      labels:
        app: api-pr
        aadpodidbinding: aadpodidentity-kube-eclinic-agentpool
      annotations:
        dapr.io/enabled: "true"
        dapr.io/log-level: "info"
        dapr.io/log-as-json: "true"
        dapr.io/id: "api-pr"
        dapr.io/port: "6000"
    spec:
      containers:
        - name: api-pr
          image: acreclinic.azurecr.io/eclinic/patientregistration:0.0.7
          ports:
            - containerPort: 6000
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: 6000
            initialDelaySeconds: 3
            periodSeconds: 3
          readinessProbe:
              httpGet:
                path: /healthz
                port: 6000
              initialDelaySeconds: 3
              periodSeconds: 3